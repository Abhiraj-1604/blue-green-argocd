apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  selector:
    app: myapp
    version: blue  # 🔁 Change to "blue" to switch traffic
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5678
