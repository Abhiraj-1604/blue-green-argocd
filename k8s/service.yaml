apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  selector:
    app: myapp
    version: green  # <--- switch between blue and green here
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
